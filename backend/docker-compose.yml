version: "3.8"

services:
  backend:
    build: .
    container_name: soil-segment-backend
    ports:
      - "9000:8000"   # external:internal
    volumes:
      - ./models:/app/models:ro
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1